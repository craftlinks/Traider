[pytest]
testpaths = src/tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
; Avoid picking up vendor tests under twsapi
norecursedirs = twsapi .venv build dist
markers =
    integration: tests that hit network or recorded cassettes
    e2e: end-to-end tests


